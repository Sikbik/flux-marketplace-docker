# Infrastructure images

Reserved for infrastructure/support services (reverse proxies, databases, monitoring, backups, etc.).

Add each image under:

- `Infrastructure/<project>/` → `Dockerfile`, `README.md`, optional `flux-spec.json`

